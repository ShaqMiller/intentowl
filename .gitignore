node_modules/
dist/
.next/
out/
.turbo/
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
*.log

# Verification builds (see NEXT_DIST_DIR in apps/web/next.config.ts)
.next-verify/

# Throwaway diagnostic scripts. Every one-off database or API probe in this
# repo is written as apps/<app>/src/_name.ts and deleted after use; this rule
# exists because two were once swept into a commit by `git add -A`.
apps/*/src/_*.ts
